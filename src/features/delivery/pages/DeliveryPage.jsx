import PlaceholderPage from "../../shared/components/PlaceholderPage.jsx";

export default function DeliveryPage() {
  return (
    <PlaceholderPage
      bullets={[
        "Zone coverage, schedules, and limits can be added later.",
        "The page is intentionally simple so it stays easy to extend.",
        "This is the same shell style used throughout the admin area.",
      ]}
      description="Delivery configuration and zone management live here."
      title="Delivery settings"
    />
  );
}
