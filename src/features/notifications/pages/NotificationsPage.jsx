import PlaceholderPage from "../../shared/components/PlaceholderPage.jsx";

export default function NotificationsPage() {
  return (
    <PlaceholderPage
      bullets={[
        "Alert routing, unread activity, and delivery preferences can live here.",
        "This route is included so the admin navigation matches the target interface cleanly.",
      ]}
      description="Review platform notifications and admin communication preferences."
      title="Notifications"
    />
  );
}
