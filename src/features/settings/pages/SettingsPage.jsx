import PlaceholderPage from "../../shared/components/PlaceholderPage.jsx";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      bullets={[
        "Admin preferences and platform defaults can be edited here.",
        "Permission controls and branding options can be placed in separate cards.",
        "This is a neat placeholder with the final structure already in place.",
      ]}
      description="Configure platform defaults, admin preferences, and high-level settings."
      title="Admin settings"
    />
  );
}
