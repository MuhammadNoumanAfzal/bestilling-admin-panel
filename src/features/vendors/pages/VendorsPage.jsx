import PlaceholderPage from "../../shared/components/PlaceholderPage.jsx";

export default function VendorsPage() {
  return (
    <PlaceholderPage
      bullets={[
        "Vendor approvals can live here later.",
        "Menu verification and compliance checks can be added next.",
        "This page already matches the admin shell and routing structure.",
      ]}
      description="Use this area for vendor onboarding, approvals, store status, and performance review. Right now it is a clean placeholder so the folder structure and routing are in place."
      title="Vendor management"
    />
  );
}
